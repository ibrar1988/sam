export interface IC0036 {
    tests_container?: StandardizedTestEntity[] | null;
  }
  export interface StandardizedTestEntity {
    container_id: string;
    container_code: string;
    visible: boolean;
  }